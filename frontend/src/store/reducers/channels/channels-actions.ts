import { createAction } from '@reduxjs/toolkit';

export const getChannelsAction = createAction('getChannelsAction');
export const setSelectedChannelAction = createAction('setSelectedChannelAction');
